# vramfit

Estimate whether an LLM inference workload will fit in GPU memory.

## Development

```console
uv sync
uv run vramfit --help
uv run python -m unittest discover
```

The estimator is not implemented yet; this is the initial CLI scaffold.
